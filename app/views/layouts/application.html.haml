!!!
%html
  %head
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
      %title= content_for?(:title) ? yield(:title) : "psn - private sharing net"
      %meta{:content => content_for?(:description) ? yield(:description) : "private sharing net", :name => "description"}
        = stylesheet_link_tag "application", :media => "all"
        = javascript_include_tag "application"
        = csrf_meta_tags
        = yield(:head)
  %body{:class => "#{controller_name} #{action_name}"}
    = render 'layouts/navbar'
    #main{:role => "main"}
      .container
        .content
          .row
            .span9
              = render 'layouts/messages'
              = yield
            .span3
              %h2 What is this?
              #sidebar_content
                Lorem Ipsum
          %footer
      / ! end of .container
    / ! end of #main
